spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
#    url: jdbc:mysql://localhost:3306/dgit?serverTimezone=UTC&characterEncoding=UTF-8
#    username: root
    password: hong3622!
#    password: wlals0330
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

app:
  jwt:
    key: jwt_auth_secret_key_for_login_user

logging:
  level:
    org.springframework.boot.autoconfigure: ERROR